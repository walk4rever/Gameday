# Guandan（掼蛋）

家庭联机掼蛋。手机浏览器打开即玩，人数不够由机器人补位，对新手做出牌限制与提示。

---

## 1. 背景与目标

**使用者**：Rafael 和孩子，2 人为主，偶尔更多。
**核心痛点**：双方都不熟掼蛋规则，需要软件承担"裁判 + 教练"两个角色。
**成功标准**：孩子能在没有大人讲解的情况下，独立打完一整局并逐渐学会规则。

### 需求映射

| # | 原始需求 | 落地方式 |
|---|---|---|
| 1 | 1–4 人可玩，不足由机器补位 | 房间固定 4 座，空位由启发式 bot 接管 |
| 2 | 手机端易用、简洁 | 竖屏 PWA，点选出牌，三按钮操作 |
| 3 | 限制非法出牌 + 适度提示 | 同一个 `getLegalPlays()` 的两个出口 |
| 4 | 记录每人战绩 | 玩家 ID 绑定战绩，存 Durable Object SQLite |

---

## 2. 核心设计判断

### 2.1 难点在规则引擎，不在界面

掼蛋牌型包含：单张、对子、三张、三带二、顺子、三连对（木板）、三同连张（钢板）、炸弹（4~n 张）、同花顺、四大天王。再叠加**级牌**（当前打的点数插到 A 之上）与**逢人配**（红桃级牌作万能牌），合法性判定是一个带通配符的组合搜索问题。

**这是整个项目风险最高的部分，必须先写测试再写实现。**

### 2.2 一个函数撑起三处

```ts
getLegalPlays(hand: Card[], lastPlay: Play | null, level: Rank): Play[]
```

| 消费方 | 用途 | 满足需求 |
|---|---|---|
| UI | 非法组合置灰 | 3a 限制 |
| UI | 排序后高亮推荐 | 3b 提示 |
| bot | 候选动作空间 | 1 补位 |
| server | 出牌合法性校验 | 防作弊 |

引擎只写一次，四处受益。这是架构的中心。

### 2.3 服务端权威

手牌**不下发全量**。每个客户端只收到自己的手牌 + 其他人的剩余张数。否则打开 DevTools 就能看到所有人的牌。

客户端本地跑一份 `rules` 做即时反馈（零延迟），服务端收到出牌后**再校验一遍**。

### 2.4 一个房间 = 一个 Durable Object

掼蛋是有状态的长会话。Durable Object 天然是"一个房间一个实例、状态常驻内存、原生 WebSocket"的模型，4 个玩家连进同一个对象，不存在多实例状态同步问题。空闲自动 hibernate。

---

## 3. 技术方案

### 3.1 部署

全部 Cloudflare，域名 `gd.air7.fun`。

**为什么不用 Vercel**：Vercel Functions 无状态、有执行时限，无法持有 WebSocket 长连接，也无法在内存中保存牌局状态；文件系统临时，SQLite 存战绩不可行。

**成本**：Workers 免费计划自 2025-04 起包含 Durable Objects（仅 SQLite 后端）。免费额度 100,000 请求/天、5 GB 存储。计费的是**进入** DO 的上行消息，广播不额外计费；一晚上牌局约数千次请求，距上限两个数量级。**长期零成本**，超额直接报错而非扣费。

### 3.2 目录结构

```
Guandan/
├── packages/
│   ├── rules/          牌型判定、大小比较、合法出牌枚举。纯 TS，零依赖
│   ├── bot/            启发式 AI，依赖 rules
│   ├── engine/         单局状态机（发牌/出牌/过牌/一局结束），依赖 rules
│   └── protocol/       客户端 <-> Room DO 的 WebSocket 消息类型，依赖 rules + engine
├── apps/
│   ├── server/         Worker + Durable Object（房间 + WebSocket + 战绩），跑 engine + bot
│   └── web/            Vite + React，PWA，唯一的产品界面（联机）
├── pnpm-workspace.yaml
└── PRODUCT.md
```

`rules`、`bot`、`engine` 是纯 TS、零依赖、不碰网络 —— **更换宿主不影响这几个包一行代码**。`engine`（牌局状态机）和 `bot`（出牌策略）只跑在 `apps/server` 里，是服务端权威的一部分；`apps/web` 只依赖 `rules` 做本地即时校验/高亮（见 §2.3），不直接跑对局状态机。

实际没有引入 Tailwind（P1 决定，见下方进度记录）——配置成本 vs 当前收益不划算，用了一份精简 CSS，P4 打磨视觉时再考虑。

### 3.3 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | TypeScript | 前后端共用 rules 包 |
| 包管理 | pnpm workspace | 本机已装 pnpm 10 |
| 前端 | Vite + React + Tailwind | 构建快，静态资源托管免费 |
| 实时 | Durable Object 原生 WebSocket | 无需 Socket.IO |
| 存储 | DO 内置 SQLite | 战绩量小，不引入额外数据库 |
| 测试 | Vitest | rules 包测试驱动 |

---

## 4. 规则范围

实现**完整掼蛋**：2v2 对抗、级牌递进、全牌型、逢人配、进贡还贡、打到 A 结束。

两处做成**房间开关**，新手局可关闭：

- **进贡 / 还贡** —— 规则绕，对新手是纯粹的认知负担
- **逢人配** —— 万能牌会让"这手牌能不能出"变得难以直觉判断

默认新手局两项均关闭，熟练后开启。

---

## 5. 交互设计

### 5.1 移动端要点

- 进房间先是**大厅**：看到几个人在线（在线/离线代打/机器人），一个"开打"按钮，按了才发牌——不是一进房间就自动开局
- 桌面按真实牌桌方位布局：**上=搭档，左右=对手**，自己固定在下方（手牌+按钮）
- 每个座位出的牌（或"过"）摆在**该玩家自己的位置**，不是一条汇总文字；一墩结束（都过完/被更大的牌压过一整圈）清空桌面
- **竖屏**为主
- 手牌单行横向滚动，**点选而非拖拽**（拖拽在小屏上极难用）
- 底部三个大按钮：**出牌 / 不要 / 提示**
- 常驻显示：当前级牌、每人剩余张数、队友标识
- 非法组合置灰时附**一句话解释**（如"对子不能压顺子"）—— 这比单纯禁用有教学价值（P4 待做，目前只有出错文案，还没有针对牌型的具体解释）

### 5.2 登录

**昵称 + 4 位 PIN**，首次输入即注册，localStorage 存 token 长期免登录。

不做账号密码体系 —— 家庭场景下是过度设计。战绩绑定这个 ID。

### 5.3 机器人

纯启发式，约 200 行。策略：

- 能压则压
- 优先出散牌
- 炸弹留到关键时刻
- 队友剩牌少时不抢
- 逢人配不浪费在小牌上

**不做 MCTS / 神经网络。** 陪孩子玩够用，真觉得太蠢再升级。

---

## 6. 分期计划

每一期结束时都是"能玩的"状态。

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **P0** | 规则规格文档 → `packages/rules` + 测试 | 测试全绿 |
| **P1** | 单机原型：本地跑通规则引擎 + bot，验证好不好玩（临时形态，不是最终产品） | 能打完一整局 ✅ |
| **P2** | 唯一的产品形态：Durable Object 房间 + 联机，1-4 人共用一个房间，空位机器人补位 | 两台手机对打 ✅（本地 `wrangler dev` 已验证端到端跑通，见下；实机部署到 `gd.air7.fun` 待做） |
| **P3** | 战绩持久化 + 排行榜 | 数据跨会话保留 |
| **P4** | 教学提示增强 | 孩子能独立看懂 |

**P1 之前不写一行网络代码。** 先确认好不好玩，再考虑联机 —— 也意味着 Cloudflare 相关工作推迟到 P2。

---

## 7. 待办与开放问题

- [x] P0 第一步：整理掼蛋规则规格文档（`packages/rules/RULES_SPEC.md`）；核心牌型判定与比较已写测试并实现（47 个测试全绿），逢人配/进贡按规格文档延后到后续里程碑
- [x] P1：新增 `packages/engine`（单局状态机：发牌/出牌/过牌/一圈过完回到领出人/一局结束判定，18 个测试，含 30 局全随机 bot 对局仿真）+ `packages/bot`（启发式出牌，8 个测试）。当时 `apps/web` 临时接了一个"本地单机模式"（1 人 + 3 bot，不联网）只是为了在写 P2 之前先验证好不好玩——**这不是产品的一部分**，P2 联机跑通之后已经删掉，产品从头到尾只有联机这一种形态，人数不足时房间内的空位由 bot 补位（1-4 人都可以，包括 1 人独自跟 3 个 bot 玩，这本来就是联机房间的一种正常人数分布，不需要为此单独做一个"离线版"）
- [x] P2：`apps/server` 实现 Room Durable Object（一个房间=一个 DO 实例，固定房间名 `default`，1-4 名真人共用，空位机器人补位）+ `packages/protocol`（WS 消息类型）+ `apps/web`（进房间前填昵称，之后就是唯一的游戏界面）。联机身份是设备本地生成的 `playerId`（localStorage），断线的座位由 bot 临时代打，重连会认回原来的座位——这是简化版，正式的昵称+PIN 跨设备账号系统留给 P3（战绩需要它才有意义）。
  - **过程中抓到一个真实的并发 bug**：两个连接几乎同时到达时，Room 各自异步读 `ctx.storage`、各自当作"房间不存在"发一副新牌，后到的把先到的覆盖掉，导致其中一个玩家的手牌和服务端权威状态对不上（"出的牌不在手上"）。用真实的两个 WebSocket 客户端（复用 `chooseBotPlay` 决策）连本地 `wrangler dev` 才复现出来——写单元测试测不出这个，因为它只在两个连接真正并发到达时才触发。修法是 Cloudflare 官方推荐的 `ctx.blockConcurrencyWhile()`：在构造函数里把状态读取包起来，保证任何请求处理之前状态已经就绪，彻底消除竞态而不是走一步)。修完后连续 6 局端到端联机仿真全部正常收尾
  - 验证方式：本地 `wrangler dev` + 两个真实 WebSocket 客户端跑完整局（含手牌隐私检查：两家收到的手牌无交集）。**没有部署到真实 Cloudflare 账号**，也没有在两台真手机上试过——这两步需要你的 Cloudflare 账号和实体设备，我这边做不了
- [ ] `gd.air7.fun` 的 DNS 当前托管在何处？若已在 Cloudflare，部署成本更低（部署本身还没做）
- [ ] 一局打到 A 结束耗时较长，是否需要"中途存档 / 下次继续"
- [ ] 是否需要观战模式（一方先出完后看剩余对局）
