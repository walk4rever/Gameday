{
  "name": "@guandan/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "pnpm --filter @guandan/web build && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@guandan/rules": "workspace:*",
    "@guandan/bot": "workspace:*",
    "@guandan/engine": "workspace:*",
    "@guandan/protocol": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
